<p id="notice"><%= notice %></p>

<h2>
  <%= @project.title %>
</h2>

<p>
  <strong>Summary:</strong>
  <%= @project.body %>
</p>

<p>
  <strong>Created:</strong>
  <%= @project.created %>
</p>

<p>
  <strong>Public:</strong>
  <%= @project.public %>
</p>
<%= link_to 'New document', new_project_document_path(@project), :type => "button", :class => 'btn btn-primary' %>
<%= link_to 'Edit', edit_project_path(@project), :type => "button", :class => 'btn btn-primary' %>
<%= link_to 'Back', projects_path, :type => "button", :class => 'btn btn-primary' %>


